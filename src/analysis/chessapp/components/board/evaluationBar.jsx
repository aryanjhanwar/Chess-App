/**
 * @deprecated Use '@/components/EvaluationBar' directly.
 *
 * The unified EvaluationBar component now supports the `currentPositionAtom`
 * and `boardOrientation` props used by the analysis app, so this file is a
 * simple re-export to preserve existing import paths without changes.
 */
export { default } from '@/components/EvaluationBar';
